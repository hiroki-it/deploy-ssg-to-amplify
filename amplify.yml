version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "APP_ENV=$APP_ENV" >> .env
        - npm install
    build:
      commands:
        - npm run generate ./dist/${UA_ENV}
        - mkdir -p ./base
        - copy -Rp ./dist/${UA_ENV} ./base
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: ./base
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
