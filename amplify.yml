version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "DEVICE_TYPE=$DEVICE_TYPE" >> .env
        - npm install
    build:
      commands:
        - set -x
        - source amplify.sh
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: ./dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
