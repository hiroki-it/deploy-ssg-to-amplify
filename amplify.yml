version: 1
frontend:
  phases:
    preBuild:
      commands:
        - set -x
        - source ./ops/amplify_prebuild.sh
    build:
      commands:
        - source ./ops/amplify_build.sh
  artifacts:
    baseDirectory: ./dist
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
